import 'package:flutter/material.dart';

class Body extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    Size size = MediaQuery.of(context)
        .size; // Isto fornece-me a totalidade da altura e largura do ecran
    return Container();
  }
}
